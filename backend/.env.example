# =====================================
# K-Now Backend 환경 변수 설정 파일
# =====================================
# 이 파일을 .env로 복사한 후 값을 채워주세요.
# 예: cp .env.example .env

# ---------------------------------
# 데이터베이스 설정 (필수)
# ---------------------------------
# SQLite 사용 (개발용)
DB_URL=sqlite+aiosqlite:///./knoti.db

# PostgreSQL 사용 (프로덕션 권장)
# DB_URL=postgresql+asyncpg://user:password@localhost:5432/knoti_db

# ---------------------------------
# AI 서비스 키 (선택)
# ---------------------------------
# Google Gemini API 키 (AI 요약 기능 사용 시 필요)
# 발급: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=

# ---------------------------------
# 푸시 알림 설정 (선택)
# ---------------------------------
# Firebase 서비스 계정 키 파일 경로
# Firebase Console에서 다운로드한 JSON 파일 경로를 입력하세요
FIREBASE_KEY_PATH=serviceAccountKey.json

# ---------------------------------
# 보안 설정 (필수)
# ---------------------------------
# 관리자 API 키 (관리자 엔드포인트 접근용)
# 설정하지 않으면 관리자 기능이 비활성화됩니다
# 예: openssl rand -hex 32 명령어로 생성 가능
ADMIN_API_KEY=your-secure-admin-api-key-here

# SSL 검증 활성화 여부 (true/false)
# 대학 웹사이트 접속 시 SSL 인증서 검증
# 프로덕션: true 권장, 개발: false 가능
SSL_VERIFY=false

# ---------------------------------
# CORS 설정 (필수)
# ---------------------------------
# 허용할 Origin (쉼표로 구분, 비어있으면 모든 origin 허용)
# 프로덕션에서는 반드시 설정하세요!
# 예: https://your-app.com,https://admin.your-app.com
ALLOWED_ORIGINS=

# ---------------------------------
# 테스트 설정 (선택)
# ---------------------------------
# 테스트 엔드포인트 활성화 여부 (true/false)
# 프로덕션에서는 false로 설정하세요
ENABLE_TEST_ENDPOINTS=false

# ---------------------------------
# 서버 설정 (선택)
# ---------------------------------
# 환경 구분 (development/production)
ENVIRONMENT=development

# 로그 레벨 (DEBUG/INFO/WARNING/ERROR)
LOG_LEVEL=INFO